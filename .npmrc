# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

; --- Peer dependency settings
; https://pnpm.io/npmrc#auto-install-peers
auto-install-peers=true ; default is false
; https://pnpm.io/npmrc#dedupe-peer-dependents
; NOTE: this is disabled as the installation speed is significantly higher
; Github issue for this: https://github.com/pnpm/pnpm/issues/6300
dedupe-peer-dependents=false ; default is true
; https://pnpm.io/npmrc#resolve-peers-from-workspace-root
resolve-peers-from-workspace-root=true ; default is true, but just to be sure

; --- Workspace settings
; https://pnpm.io/npmrc#link-workspace-packages
link-workspace-packages=true ; default is true, but just to be sure
; https://pnpm.io/npmrc#prefer-workspace-packages
prefer-workspace-packages=true ; default is false, prefer local workspace packages over registry packages
; https://pnpm.io/npmrc#save-workspace-protocol
save-workspace-protocol=rolling ; default is rolling

; --- Other settings
; https://pnpm.io/npmrc#save-prefix
save-prefix=^ ; default is ^
; https://pnpm.io/npmrc#deploy-all-files
deploy-all-files=false ; default is false, avoid having all files in the published/linked package to reduce install sizes
; https://pnpm.io/npmrc#dedupe-direct-dependents
dedupe-direct-dependents=false ; default is false, but just to be sure
